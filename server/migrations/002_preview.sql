-- Превью доски для карточки в списке.
--
-- Карточка без картинки — это строка текста: чтобы найти нужную доску, приходится
-- читать названия одно за другим. Превью делает список узнаваемым с одного взгляда.
--
-- Считает его КЛИЕНТ и присылает готовым: сервер документ не разбирает (см. boards.js),
-- и заводить в нём знание про узлы, страницы и раскладку значило бы завести вторую
-- реализацию формата, которая начнёт отставать от первой.
ALTER TABLE boards ADD COLUMN preview TEXT;

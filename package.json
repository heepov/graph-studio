{
  "name": "graph-studio",
  "version": "1.3.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "node scripts/gen-bridge.mjs && vite build && node scripts/pack-viewer.mjs dist",
    "preview": "vite preview",
    "test": "node test/smoke.js && node test/legacy.js",
    "bridge": "node scripts/gen-bridge.mjs"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  }
}

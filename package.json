{
  "name": "graph-studio",
  "version": "1.3.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/pack-viewer.mjs dist",
    "preview": "vite preview",
    "test": "node test/smoke.js && node test/legacy.js"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  }
}
